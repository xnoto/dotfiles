<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<fontconfig>
  <!--
    Font configuration for bitmap terminal fonts with emoji support.

    NOTE: The main configuration is now in conf.d/99-terminal-fonts.conf
    which loads after all system configurations.

    This file is kept for backwards compatibility but the active
    configuration is in the conf.d directory.
  -->
</fontconfig>
